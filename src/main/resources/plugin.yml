name: HideAndSeek
version: '${project.version}'
main: de.expertofclients.hideandseek.Main
api-version: '1.20'

commands:
  randompick:
    permission:
      - test.*
      - test.neu
  pickSeeker:
  stopHideandSeek:
  HasConfig:
